# CI/CD Pipeline Architecture

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        GitHub Repository                        │
│                 liberia-islamic-schools-directory               │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                ┌──────────┴──────────┐
                │                     │
         ┌──────▼──────┐      ┌──────▼──────┐
         │   develop   │      │     main    │
         │  (staging)  │      │ (production)│
         └──────┬──────┘      └──────┬──────┘
                │                     │
                └──────────┬──────────┘
                           │
          ┌────────────────┴────────────────┐
          │      GitHub Actions             │
          │   Workflow Orchestration        │
          └────────────────┬────────────────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
   ┌────▼────┐      ┌─────▼─────┐     ┌─────▼─────┐
   │ CI/CD   │      │PR Checks  │     │  Deploy   │
   │Pipeline │      │ Workflow  │     │ Workflow  │
   └────┬────┘      └─────┬─────┘     └─────┬─────┘
        │                 │                   │
        └────────┬────────┴────────┬──────────┘
                 │                 │
    ┌────────────▼────────┐   ┌────▼────────────┐
    │  Build & Test       │   │  Quality Gates  │
    │  - Frontend (Vite)  │   │  - TypeScript   │
    │  - Backend (Express)│   │  - ESLint       │
    │  - Run Tests        │   │  - Prettier     │
    │  - Security Audit   │   │  - Coverage     │
    └────────────┬────────┘   └─────────────────┘
                 │
    ┌────────────▼────────────┐
    │   Deployment Targets    │
    └────────────┬────────────┘
                 │
         ┌───────┴───────┐
         │               │
    ┌────▼────┐     ┌────▼────┐
    │ Vercel  │     │ Railway │
    │Frontend │     │Backend  │
    └────┬────┘     └────┬────┘
         │               │
         └───────┬───────┘
                 │
        ┌────────▼────────┐
        │  Health Checks  │
        │  - Frontend 200 │
        │  - Backend /api │
        └─────────────────┘
```

## 🔄 Workflow Execution Flow

### 1. Pull Request Workflow

```
Developer Creates PR
        │
        ▼
┌─────────────────┐
│  PR Triggered   │
└────────┬────────┘
         │
    ┌────▼────────────────────────────┐
    │  PR Checks Workflow Starts      │
    └────┬────────────────────────────┘
         │
    ┌────▼─────────────────────────────┐
    │ Job 1: Validate PR               │
    │ - Check PR title                 │
    │ - Check for merge conflicts      │
    └────┬─────────────────────────────┘
         │
    ┌────▼─────────────────────────────┐
    │ Job 2: Code Quality              │
    │ - TypeScript: npx tsc --noEmit   │
    │ - ESLint: npm run lint           │
    │ - Prettier: check formatting     │
    └────┬─────────────────────────────┘
         │
    ┌────▼─────────────────────────────┐
    │ Job 3: Test Coverage             │
    │ - Run all tests                  │
    │ - Generate coverage report       │
    │ - Upload to Codecov              │
    └────┬─────────────────────────────┘
         │
    ┌────▼─────────────────────────────┐
    │ Job 4: Build Verification        │
    │ - npm run build (frontend)       │
    │ - npm run build (backend)        │
    │ - Check build size               │
    └────┬─────────────────────────────┘
         │
    ┌────▼─────────────────────────────┐
    │ Job 5: Security Scan             │
    │ - npm audit (dependencies)       │
    │ - TruffleHog (secrets)           │
    └────┬─────────────────────────────┘
         │
    ┌────▼─────────────────────────────┐
    │ Job 6: Comment Results           │
    │ - Create/Update PR comment       │
    │ - Show status: ✅ or ❌          │
    └──────────────────────────────────┘
```

### 2. Main CI/CD Workflow

```
Push to main/develop
        │
        ▼
┌─────────────────┐
│ CI/CD Triggered │
└────────┬────────┘
         │
    ┌────▼─────────────────────────────┐
    │ Job 1: Lint                      │
    │ - ESLint check                   │
    │ - TypeScript type check          │
    └────┬─────────────────────────────┘
         │
         ├──────────────────────┐
         │                      │
    ┌────▼───────────┐     ┌────▼───────────┐
    │ Job 2: Build   │     │ Job 3: Build   │
    │   Frontend     │     │    Backend     │
    │                │     │                │
    │ - npm ci       │     │ - cd server    │
    │ - npm build    │     │ - npm ci       │
    │ - Upload dist/ │     │ - npm build    │
    └────┬───────────┘     └────┬───────────┘
         │                      │
         └──────────┬───────────┘
                    │
               ┌────▼─────────────────────┐
               │ Job 4: Run Tests         │
               │ - Frontend tests         │
               │ - Backend tests          │
               └────┬─────────────────────┘
                    │
               ┌────▼─────────────────────┐
               │ Job 5: Security Audit    │
               │ - npm audit (frontend)   │
               │ - npm audit (backend)    │
               └────┬─────────────────────┘
                    │
            ┌───────┴───────┐
            │               │
    Is branch main?    Is branch develop?
            │               │
            ▼               ▼
    ┌───────────────┐  ┌──────────────┐
    │ Job 6: Deploy │  │ Job 7: Deploy│
    │  Production   │  │   Staging    │
    └───────────────┘  └──────────────┘
```

### 3. Deployment Workflow

```
Manual Trigger / Push to main
        │
        ▼
┌──────────────────────┐
│ Deploy Workflow      │
└──────────┬───────────┘
           │
    ┌──────┴──────┐
    │             │
┌───▼────┐   ┌────▼────┐
│ Build  │   │  Build  │
│Frontend│   │ Backend │
└───┬────┘   └────┬────┘
    │             │
    │   Upload    │
    │  Artifacts  │
    │             │
    └──────┬──────┘
           │
    ┌──────┴──────┐
    │             │
┌───▼─────┐  ┌────▼────┐
│ Deploy  │  │ Deploy  │
│ Vercel  │  │ Railway │
└───┬─────┘  └────┬────┘
    │             │
    └──────┬──────┘
           │
      ┌────▼──────┐
      │ Wait 30s  │
      └────┬──────┘
           │
    ┌──────┴────────┐
    │               │
┌───▼────────┐ ┌────▼────────┐
│ Health     │ │  Health     │
│ Check FE   │ │  Check BE   │
│ (200 OK?)  │ │  (200 OK?)  │
└───┬────────┘ └────┬────────┘
    │               │
    └───────┬───────┘
            │
       ┌────▼────┐
       │ Success?│
       └────┬────┘
            │
     ┌──────┴──────┐
     │             │
    Yes           No
     │             │
┌────▼────┐   ┌────▼────┐
│ Notify  │   │Rollback │
│ Success │   │ Trigger │
└─────────┘   └─────────┘
```

## 🏢 Deployment Architecture

```
┌────────────────────────────────────────────────────────┐
│                     Users / Browsers                   │
└──────────────────────┬─────────────────────────────────┘
                       │
            ┌──────────┴──────────┐
            │                     │
    ┌───────▼────────┐    ┌──────▼──────────┐
    │   Frontend     │    │    Backend      │
    │   (Vercel)     │◄──►│   (Railway)     │
    │                │    │                 │
    │ - React App    │    │ - Express API   │
    │ - Static Files │    │ - REST API      │
    │ - CDN Cached   │    │ - PostgreSQL    │
    └────────────────┘    └─────────┬───────┘
                                    │
                          ┌─────────▼─────────┐
                          │   PostgreSQL DB   │
                          │   (Railway)       │
                          │                   │
                          │ - Schools Data    │
                          │ - Users Data      │
                          │ - Auth Tokens     │
                          └───────────────────┘
```

## 📊 Environment Flow

```
┌─────────────┐
│ Development │  (Local)
└──────┬──────┘
       │ git push
       ▼
┌─────────────┐
│   Staging   │  (develop branch)
└──────┬──────┘
       │ merge to main
       ▼
┌─────────────┐
│ Production  │  (main branch)
└─────────────┘

Development:
- URL: localhost:5173 / localhost:4000
- Database: Local PostgreSQL
- API Key: Development keys

Staging:
- URL: staging.liberia-islamic-schools.vercel.app
- Database: Staging PostgreSQL (Railway)
- API Key: Staging keys

Production:
- URL: liberia-islamic-schools.vercel.app
- Database: Production PostgreSQL (Railway)
- API Key: Production keys
```

## 🔐 Secrets Management Flow

```
┌────────────────────────┐
│   GitHub Repository    │
│   Settings → Secrets   │
└───────────┬────────────┘
            │
    ┌───────┴───────┐
    │               │
┌───▼────────┐ ┌────▼──────────┐
│ Vercel     │ │   Railway     │
│ Secrets    │ │   Secrets     │
│            │ │               │
│ - Token    │ │ - Token       │
│ - Org ID   │ │ - Project ID  │
│ - Proj ID  │ │ - DB URL      │
└────────────┘ └───────────────┘
```

## 🚦 Quality Gates

```
Code Commit
    │
    ▼
┌────────────┐
│ TypeScript │ ──► Pass? ──► Continue
│ Type Check │          │
└────────────┘          └──► Fail ──► ❌ Block
    │
    ▼
┌────────────┐
│  ESLint    │ ──► Pass? ──► Continue
│  Linting   │          │
└────────────┘          └──► Fail ──► ❌ Block
    │
    ▼
┌────────────┐
│   Tests    │ ──► Pass? ──► Continue
│  Coverage  │          │
└────────────┘          └──► Fail ──► ❌ Block
    │
    ▼
┌────────────┐
│   Build    │ ──► Pass? ──► Continue
│Verification│          │
└────────────┘          └──► Fail ──► ❌ Block
    │
    ▼
┌────────────┐
│  Security  │ ──► Pass? ──► Continue
│   Audit    │          │
└────────────┘          └──► Fail ──► ⚠️ Warn
    │
    ▼
  ✅ Ready to Deploy
```

## 🎯 Deployment Targets Matrix

| Environment | Branch   | Frontend URL                          | Backend URL                              | Auto Deploy |
|-------------|----------|---------------------------------------|------------------------------------------|-------------|
| Local       | any      | localhost:5173                        | localhost:4000                           | No          |
| Staging     | develop  | staging.liberia-islamic-schools.com   | staging-api.railway.app                  | Yes         |
| Production  | main     | liberia-islamic-schools.vercel.app    | api.liberia-islamic-schools.railway.app  | Yes         |

## 📈 Monitoring & Alerts

```
┌─────────────────┐
│   Deployment    │
└────────┬────────┘
         │
    ┌────▼────────────────┐
    │  Health Checks      │
    │  - Every 30s        │
    │  - HTTP Status 200  │
    └────────┬────────────┘
             │
      ┌──────┴──────┐
      │             │
   Success       Failure
      │             │
┌─────▼─────┐  ┌────▼─────┐
│  GitHub   │  │  GitHub  │
│  Actions  │  │  Actions │
│  ✅ Pass  │  │  ❌ Fail │
└───────────┘  └────┬─────┘
                    │
              ┌─────▼──────┐
              │  Rollback  │
              │  Triggered │
              └────────────┘
```

---

**Legend:**
- `│` `▼` `►` `─` - Flow direction
- `┌─┐` `└─┘` - Boxes/Components
- `✅` - Success state
- `❌` - Failure/Block state
- `⚠️` - Warning state
- `◄─►` - Bidirectional connection
